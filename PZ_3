# 1. Даны числа х, у, x1, y1, х2, у2. Проверить истинность высказывания: «Точка с
# координатами (х, у) лежит внутри прямоугольника, левая верхняя вершина которого
# имеет координаты (х1, у1), правая нижняя — (х2, у2), а стороны параллельны
# координатным осям».

while True:
    try:
        x, y, x1, y1, x2, y2 = int(input('Введите число для x: ')), int(input('Введите число для y: ')), int(input('Введите число для x1: ')), int(input('Введите число для y1: ')), int(input('Введите число для x2: ')), int(input('Введите число для y2: '))
        left = min(x1, x2)
        right = max(x1, x2)
        top = max(y1, y2)
        bottom = min(y1, y2)
        if left <= x <= right and bottom <= y <= top:
            print("Точка внутри или на границе прямоугольника.")
        else:
            print("Точка вне прямоугольника.")
        break   
    except ValueError:
        print('Введите целое число')
